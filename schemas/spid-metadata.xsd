<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           xmlns:md="urn:oasis:names:tc:SAML:2.0:metadata"
           targetNamespace="urn:oasis:names:tc:SAML:2.0:metadata"
           elementFormDefault="qualified"
           attributeFormDefault="unqualified">

  <!-- Include dello schema base SAML Metadata -->
  <xs:include schemaLocation="saml-schema-metadata-2.0.xsd"/>

  <!-- Restrizioni SPID -->
  <xs:simpleType name="BindingType">
    <xs:restriction base="xs:anyURI">
      <xs:enumeration value="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect"/>
      <xs:enumeration value="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"/>
    </xs:restriction>
  </xs:simpleType>

</xs:schema>
